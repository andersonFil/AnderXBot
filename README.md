# AnderX Bot 🦊

AnderX Bot es un bot de WhatsApp desarrollado en **Node.js** utilizando la librería [Baileys](https://github.com/WhiskeySockets/Baileys).  
Su objetivo es ofrecer automatización, menús dinámicos y herramientas útiles para grupos y comunidades.

---

## 🚀 Características principales
- Comando `!help` → muestra la lista de comandos disponibles.
- Comando `!mp4` y `!mp3` → descarga y convierte videos/audio.
- Comando `!pi` / `!p` → búsqueda y envío de imágenes.
- Comando `!estado` → estadísticas del bot (anti-link, whitelist, etc.).
- Comando `!limpiar` → borra archivos temporales (solo admin).
- Comando `!nn` → contenido adulto (desactivado por seguridad).
- Sistema **anti-link configurable** con whitelist y controles de administrador.
- Menús dinámicos y mensajes épicos para grupos.
- Mascota personalizada: 🦊 **White Fox**.

---

## 📦 Instalación local (Termux / Linux / VPS)

1. Clona el repositorio:
   ```bash
   git clone https://github.com/andersonFil/AnderXBot.git
   cd AnderXBot
